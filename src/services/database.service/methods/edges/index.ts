import { getEdges } from './getEdges'
import { getEdgesCount } from './getEdgesCount'
import { getEdge } from './getEdge'
import { createEdge } from './createEdge'
import { updateEdge } from './updateEdge'
import { deleteEdge } from './deleteEdge'
import { restoreEdge } from './restoreEdge'
export const edges = {
  getEdges,
  getEdgesCount,
  getEdge,
  createEdge,
  updateEdge,
  deleteEdge,
  restoreEdge,
}